Explanation:
The HomeTheaterFacade simplifies the interaction with the Amplifier, DVDPlayer, and Projector classes.
The client interacts only with the facade, which internally manages the subsystem classes.
This pattern is useful for reducing dependencies and making the system easier to use.