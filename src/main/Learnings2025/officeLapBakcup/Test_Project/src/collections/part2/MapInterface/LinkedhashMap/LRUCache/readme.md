A Least Recently Used (LRU) cache is a type of cache that discards the least recently used items first when the cache is full. It's a popular caching algorithm used to manage limited-size caches efficiently


