
Solution 1 : Time complexity O(n)
1. we create an empty array to store values without duplicates,
2. iterate over array, and store the element if its not existing in new array.
            pseudo code :
                for elements in arr:
                    if element not in newArray :
                        newArray.append(element).


- using hashtable.
